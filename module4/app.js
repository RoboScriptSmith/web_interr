// app.js

// Import the lodash module
const _ = require('lodash');

// Use lodash to perform an operation
const array = [1, 2, 3, 4, 5];
const shuffledArray = _.shuffle(array);

console.log('Shuffled array:', shuffledArray);
